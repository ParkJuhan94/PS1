#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <algorithm>

int main() {
	int numStu;
	int a[10] = { 0 };
	int answer[10] = { 0 };
	int i;

	scanf("%d", numStu);

	for (i = 0; i < numStu; i++) {
		scanf("%d %d", &a[i], &answer[i]);
	}




}